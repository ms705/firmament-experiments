diff --git a/testing/test.js b/testing/test.js
index d0d8ee9..de9ad55 100644
--- a/testing/test.js
+++ b/testing/test.js
@@ -143,22 +143,19 @@ function testImage(image, callback) {
 }
 
 function printDots() {
-  charm.erase("start");
-  charm.move(-count, 0);
-
   var str = "";
   for (var i = 0; i < results.length; i++) {
     if (results[i] == "pass") {
-      str += "•".green.bold;
+      str += "^";
     }
     else  {
-      str += "•".red.bold;
+      str += "v";
     }
   }
   var rest = count - results.length;
 
   for (var i = 0; i < rest; i++) {
-    str += "·".grey;
+    str += "-";
   }
-  charm.write(str);
+  console.log(str);
 }
