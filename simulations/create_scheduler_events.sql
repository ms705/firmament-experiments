CREATE TABLE scheduler_events (
  timestamp INTEGER primary key,
  sched_time INTEGER,
  algo_time INTEGER,
  total_time INTEGER,
  unscheduled_tasks INTEGER,
  evicted_tasks INTEGER,
  migrated_tasks INTEGER,
  total_tasks INTEGER,
  task_events_per_round INTEGER,
  machine_events_per_round INTEGER,
  nodes_added INTEGER,
  nodes_removed INTEGER,
  arcs_added INTEGER,
  arcs_changed INTEGER,
  arcs_removed INTEGER,
  add_task_node INTEGER,
  add_resource_node INTEGER,
  add_equiv_class_node INTEGER,
  add_unsched_job_node INTEGER,
  add_sink_node INTEGER,
  add_arc_task_to_equiv_class INTEGER,
  add_arc_task_to_res INTEGER,
  add_arc_equiv_class_to_res INTEGER,
  add_arc_between_equiv_class INTEGER,
  add_arc_between_res INTEGER,
  add_arc_to_unsched INTEGER,
  add_arc_from_unsched INTEGER,
  add_arc_running_task INTEGER,
  add_arc_res_to_sink INTEGER,
  del_unsched_job_node INTEGER,
  del_task_node INTEGER,
  del_resource_node INTEGER,
  del_equiv_class_node INTEGER,
  del_arc_equiv_class_to_res INTEGER,
  del_arc_running_task INTEGER,
  del_arc_evicted_task INTEGER,
  del_arc_between_equiv_class INTEGER,
  del_arc_between_res INTEGER,
  del_arc_task_to_equiv_class INTEGER,
  del_arc_task_to_res INTEGER,
  del_arc_res_to_sink INTEGER,
  chg_arc_evicted_task INTEGER,
  chg_arc_to_unsched INTEGER,
  chg_arc_from_unsched INTEGER,
  chg_arc_task_to_equiv_class INTEGER,
  chg_arc_equiv_class_to_res INTEGER,
  chg_arc_between_equiv_class INTEGER,
  chg_arc_between_res INTEGER,
  chg_arc_running_task INTEGER,
  chg_arc_task_to_res INTEGER,
  chg_arc_res_to_sink INTEGER
);
